version: '3.9'

services:

  api:
    build: ./backend
    container_name: aasi_api
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"